import { View, Text } from 'react-native'
import React from 'react'

const SearchEvent = ({navigation,route}:any) => {
  return (
    <View>
      <Text>SearchEvent</Text>
    </View>
  )
}

export default SearchEvent